def high_and_low(numbers):
	new = numbers.split()
	the = [int(i) for i in new]
	
	big = str(max(the))
	small = str(min(the))

	return (big + ' '+ small)
  
  
  #script that returns highest and lowest value from a string of numbers
